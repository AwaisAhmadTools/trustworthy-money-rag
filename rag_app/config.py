"""Shared constants"""

# --- Expected pipeline outputs (assertions in smoke/verify scripts) ---
EXPECTED_CHUNKS = 1135   # current corpus × current chunk config — update DELIBERATELY
TEST_QUERY = "What is murabaha and how does it differ from a conventional loan?"